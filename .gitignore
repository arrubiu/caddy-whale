# VSCode
.vscode/
*.code-workspace

# JetBrains (IntelliJ / PyCharm / ecc.)
.idea/
*.iml

# Claude Code — config locale della macchina, non condivisa
.claude/settings.local.json

# macOS
.DS_Store

# Editor swap/backup
*.swp
*.swo
*~

# Secrets — mai committare la API key CrowdSec
.env
.env.*
!.env.example

# Secrets — mai committare hash password reali degli utenti Authelia
authelia/users_database.yml

# Secrets — file dei segreti Authelia (jwt/session/storage/oidc/chiave issuer)
authelia/secrets/

# Secrets — client OIDC reali (Drupal/Grafana/Portainer, quando attivati)
authelia/oidc.yml
